import { RegisterDDirective } from './register-d.directive';

describe('RegisterDDirective', () => {
  it('should create an instance', () => {
    const directive = new RegisterDDirective();
    expect(directive).toBeTruthy();
  });
});
