import { RegisterPipePipe } from './register-pipe.pipe';

describe('RegisterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RegisterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
